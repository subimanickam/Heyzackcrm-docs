# Copy to `.env.local` as needed. `NEXT_PUBLIC_*` is inlined at build time.

# Canonical site URL (OG images, llms.txt links). Falls back to Vercel env or `lib/theme-config` defaults.
# NEXT_PUBLIC_SITE_URL=https://docs.example.com
