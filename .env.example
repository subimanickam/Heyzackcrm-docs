# HeyZack CRM docs — sibling Partner Portal docs URLs
# Copy to .env.local and adjust. NEXT_PUBLIC_* is inlined at build time.

# Which site this build is: `crm` (default here) or `partner`
# NEXT_PUBLIC_DOCS_SITE=crm

# Full URL to the Partner Portal documentation app (include /docs if that is your docs base path)
# Local default if unset: http://localhost:3000/docs
# NEXT_PUBLIC_PARTNER_DOCS_URL=https://partner-docs.example.com/docs

# Full URL to this CRM documentation app (used when viewing Partner site and linking “CRM”)
# Local default if unset: http://localhost:3001/docs
# NEXT_PUBLIC_CRM_DOCS_URL=https://crm-docs.example.com/docs
